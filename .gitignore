# Segredos e config local do Claude Code — NUNCA versionar (contém tokens ao vivo)
.claude/settings.json
.claude/settings.local.json
.claude/scheduled_tasks.lock

# Dados de runtime do Home Assistant (criados no aparelho) — nunca versionar
hass-config/
hass-venv/
haconfig/
data/
container_data/
.storage/
home-assistant.log*
*.db
*.db-shm
*.db-wal

# Segredos genéricos
*.env
!*.env.example
# source.env é script essencial (funções udocker legadas), sem segredos — versionar
!source.env
!legacy-udocker/source.env
secrets.yaml
*.key

# Node
node_modules/

# OS
desktop.ini
.DS_Store
Thumbs.db
